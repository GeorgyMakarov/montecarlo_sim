stk_ret <- function(stk_prc   = NULL,
                    n_periods = 1,
                    mn_ret    = NULL,
                    sd_ret    = NULL){
  delta_t <- 1 / n_periods
  for (i in seq(n_periods)){
    eps 
  }
  
}